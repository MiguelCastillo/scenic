/**
 * Shim for fileformats that get bundled in a way that can run in web workers.
 */
import {Timer} from "../../packages/utils/timer.js";
import {ObjFile} from "@scenic/obj";

export {Timer, ObjFile};
